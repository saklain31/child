<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saklain.child">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NewBorn"
            android:label="@string/app_name" />
        <activity
            android:name=".ChildrenGrowth"
            android:label="@string/app_name" />
        <activity
            android:name=".GrowthSteps"
            android:label="@string/app_name" />
        <activity android:name=".Education" />
        <activity android:name=".HelpLine"></activity>
        <activity android:name=".PregnancyTime" />
        <activity android:name=".HealthCare"></activity>
    </application>

</manifest>